apiVersion: v1
kind: Service
metadata:
  name: frontend-web-client
  namespace: smart-healthcare
spec:
  type: NodePort
  selector:
    app: frontend-web-client
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30013