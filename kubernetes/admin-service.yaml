apiVersion: v1
kind: Service
metadata:
  name: admin
  namespace: smart-healthcare
spec:
  selector:
    app: admin
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30174
  type: NodePort
